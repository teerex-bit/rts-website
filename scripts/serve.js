const http=require('http'),fs=require('fs'),path=require('path');const root=path.join(__dirname,'..','public');http.createServer((q,r)=>{let p=decodeURIComponent(q.url.split('?')[0]);p=path.join(root,p);if(p.endsWith('/'))p+='index.html';fs.readFile(p,(e,b)=>{r.statusCode=e?404:200;r.setHeader('Content-Type',p.endsWith('.css')?'text/css':p.endsWith('.js')?'text/javascript':p.endsWith('.svg')?'image/svg+xml':'text/html');r.end(e?'Not found':b)})}).listen(process.env.PORT||4173,()=>console.log('http://localhost:4173'));
